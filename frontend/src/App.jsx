export default function App() {
    return (
        <main className="min-h-screen bg-slate-100 text-slate-900 p-6">
            <h1 className="text-2xl font-bold">AI Agent Frontend</h1>
            <p className="mt-2">Vite + React + Zustand + react-window scaffold is ready.</p>
        </main>
    );
}
